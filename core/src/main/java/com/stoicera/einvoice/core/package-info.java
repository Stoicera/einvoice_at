/**
 * Canonical invoice domain model (EN 16931 core subset).
 *
 * <p>This module must depend on nothing but the JDK — no Spring, no JPA, no format libraries.
 * Enforced by ArchUnit rules from milestone M1 on.
 */
package com.stoicera.einvoice.core;
