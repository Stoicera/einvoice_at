/**
 * Mapping between the canonical model and the concrete formats (ebInterface 6.1, UBL BIS 3.0).
 *
 * <p>MapStruct where mechanical, hand-written and heavily tested where semantic. Golden-file tests
 * are the contract. Must not import Spring.
 */
package com.stoicera.einvoice.mapping;
